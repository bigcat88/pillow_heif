name: Test

on:
  pull_request:
    paths:
      - '.github/workflows/test.yml'
      - 'pillow_heif/*.*'
      - 'tests/**'
      - 'setup.*'
      - 'pyproject.toml'
      - '.pre-commit-config.yaml'
  push:
    branches: [master]
    paths:
      - '.github/workflows/test.yml'
      - 'pillow_heif/*.*'
      - 'tests/**'
      - 'setup.*'
      - 'pyproject.toml'
      - '.pre-commit-config.yaml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # build the aom decoder as a loadable plugin for `test_load_plugin`
  PH_LIBHEIF_CMAKE_ARGS: "-DENABLE_PLUGIN_LOADING=ON -DWITH_AOM_DECODER=ON -DWITH_AOM_DECODER_PLUGIN=ON"

jobs:
  lint:
    runs-on: ubuntu-24.04
    name: Analysis

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Restore dependencies cache
        id: deps-cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ runner.temp }}/ph-deps
          key: deps-test-plugins-${{ hashFiles('libheif/build_libs.py') }}

      - name: Install Libheif
        run: |
          sudo apt update && sudo apt -y install nasm libaom-dev
          sudo BUILD_DIR="${{ runner.temp }}/ph-deps" PH_LIBHEIF_CMAKE_ARGS="$PH_LIBHEIF_CMAKE_ARGS" python3 libheif/build_libs.py

      - name: Save dependencies cache
        if: steps.deps-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ runner.temp }}/ph-deps
          key: deps-test-plugins-${{ hashFiles('libheif/build_libs.py') }}

      - name: Install from source
        run: python3 -m pip install ".[dev]" wheel setuptools

      - name: Run Analysis
        run: python3 -m pylint "setup.py" "pillow_heif/"

  pillow-dev:
    runs-on: ubuntu-24.04
    name: Pillow-dev • 🐍3.12

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Prepare system
        run: |
          sudo apt update
          sudo apt -y install nasm libaom-dev
          sudo apt -y install zlib1g-dev libjpeg-dev liblcms2-dev libwebp-dev libfribidi-dev libharfbuzz-dev libffi-dev

      - name: Restore dependencies cache
        id: deps-cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ runner.temp }}/ph-deps
          key: deps-test-plugins-${{ hashFiles('libheif/build_libs.py') }}

      - name: Install Libheif
        run: sudo BUILD_DIR="${{ runner.temp }}/ph-deps" PH_LIBHEIF_CMAKE_ARGS="$PH_LIBHEIF_CMAKE_ARGS" python3 libheif/build_libs.py

      - name: Save dependencies cache
        if: steps.deps-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ${{ runner.temp }}/ph-deps
          key: deps-test-plugins-${{ hashFiles('libheif/build_libs.py') }}

      - name: Prepare libheif plugin for load test
        run: |
          PLUGIN_SO=$(find /usr/lib -name "libheif-aomdec.so" -print -quit)
          sudo mv "$PLUGIN_SO" "$GITHUB_WORKSPACE/"
          echo "TEST_PLUGIN_LOAD=$GITHUB_WORKSPACE/libheif-aomdec.so" >> "$GITHUB_ENV"

      - name: Install from source
        run: |
          git clone --depth=1 https://github.com/python-pillow/Pillow.git
          cd Pillow && python3 -m pip install . && cd ..
          python3 -m pip -v install ".[dev]"

      - name: LibHeif info
        run: python3 -c "import pillow_heif; print(pillow_heif.libheif_info())"

      - name: Generate coverage report
        run: coverage run -m pytest && coverage xml

      - name: Upload report to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v6.0.2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: pillow-dev
          fail_ci_if_error: false

  import-error:
    runs-on: macos-14
    name: ImportError

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Install Libheif
        run: |
          brew update
          brew install libheif

      - name: Install from source
        run: |
          python3 -m pip -v install .
          python3 -m pip install pytest coverage

      - name: Generate coverage report
        run: coverage run -m pytest tests/import_error_test.py && coverage xml

      - name: Upload report to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v6.0.2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: import-error
          fail_ci_if_error: false
